syntax = "proto3";

package endpoints;

import "models/registered_function.proto";

message CheckFunctionRequest {
  optional models.RegisteredFunction registered_function = 1;
}

// A successful response indicates existence.
// If the function does not exist a NOT_FOUND status will be thrown.
message CheckFunctionResponse {
}
