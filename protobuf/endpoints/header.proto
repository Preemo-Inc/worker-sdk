syntax = "proto3";

package endpoints;

import "models/status.proto";

message HeaderRequest {
  optional string version = 1;
}

message HeaderResponse {
  optional models.Status status = 1;
  optional string message = 2;
}
