syntax = "proto3";

import "shared/status.proto";

message HeaderRequest {
  optional string version = 1;
}

message HeaderReply {
  optional Status status = 1;
  optional string message = 2;
}

service Header {
  rpc CheckHeader (HeaderRequest) returns (HeaderReply) {}
}